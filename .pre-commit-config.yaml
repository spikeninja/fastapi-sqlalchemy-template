repos:
  - repo: local
    hooks:
      - id: pyright-uv
        name: pyright type checks
        entry: uv run pyright web/app web/tests
        language: system
        stages: [pre-commit]
        pass_filenames: false